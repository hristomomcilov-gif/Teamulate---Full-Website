# Preview draft overlay only. Extract the preview-team bundle into
# teamulate.ca/preview/team/ - never into the document root and never over /team/.
Options -Indexes
DirectoryIndex index.html

<IfModule mod_headers.c>
  Header set X-Robots-Tag "noindex, nofollow"
</IfModule>
